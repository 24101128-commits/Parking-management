# Parking-management
Nơi nộp code tiến độ
--Cần cài trong terminal
python -m pip install --upgrade pip
pip install easyocr
--rồi cài tiếp
pip install easyocr ultralytics opencv-python pillow qrcode[pil] torch torchvision torchaudio

--tạo tệp requirements.txt 
xong dán
easyocr
ultralytics
opencv-python
Pillow
qrcode[pil]
torch
torchvision
--rồi cài tiếp
pip install -r requirements.txt
